class PalPermute {
    // 1.4 - Find all permutations of palindromes derived from a given string.

    public static String[] isPalidromPermutation(String s){
        // Get every combination
        // Check if it is a palindrome
        return new String[]{ "test", "test", "test" } ;
    }
}
